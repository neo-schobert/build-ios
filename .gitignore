# Xcode cache (regénéré par Xcode automatiquement)
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/

# OS
.DS_Store
Thumbs.db

# Pods (si tu uses CocoaPods, la CI les réinstalle)
# Pods/   ← garde cette ligne commentée si tu as un Podfile.lock